/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
  images: {
    domains: ["127.0.0.1", "orchid-oasis.onrender.com", "i.ibb.co", "i.ibb.co.com"],
  },
};

export default nextConfig;
